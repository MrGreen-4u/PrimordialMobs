org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false
jei_version=15.20.0.116
jei_mc_version=1.20.1

# Dev-client identity: set these to see your real skin in runClient (see build.gradle).
# mc_username=YourInGameName
# mc_uuid=your-profile-uuid-without-dashes
